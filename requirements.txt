# Direct dependencies. Mirrors [tool.poetry.dependencies] in pyproject.toml.
# This file was empty, so the README's documented install path installed
# nothing and left the app unimportable.
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Caching==2.4.1
SQLAlchemy==2.0.52
alembic==1.19.1
requests==2.34.2
psycopg2-binary==2.9.12

# GitPython is no longer used by any live code: repolens/clones.py and
# repolens/packager.py invoke git directly, which they must in order to reach
# --mirror, ls-tree -z, and blame --line-porcelain. The only remaining import
# is in utils/converter.py, which nothing imports. Drop this line when
# utils/ goes.
GitPython==3.1.59

# Optional: required only by POST /api/screenshot, imported lazily so the
# rest of the application runs without them.
selenium==4.47.0
webdriver-manager==4.1.2
