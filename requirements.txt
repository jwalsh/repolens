# Direct dependencies. Mirrors [tool.poetry.dependencies] in pyproject.toml.
# This file was empty, so the README's documented install path installed
# nothing and left the app unimportable.
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Caching==2.4.1
SQLAlchemy==2.0.52
alembic==1.19.1
requests==2.34.2
psycopg2-binary==2.9.12

# gitignore pattern semantics for the vendored/generated heuristics. Used for
# matching only -- hierarchical resolution is delegated to git itself, which
# is the part tools get subtly wrong. See repolens/ignores.py.
pathspec==1.1.1

# No GitPython. repolens/clones.py and repolens/packager.py invoke git
# directly, which they must in order to reach --mirror, ls-tree -z, and
# blame --line-porcelain. It went with utils/, its only importer.

# Optional: required only by POST /api/screenshot, imported lazily so the
# rest of the application runs without them.
selenium==4.47.0
webdriver-manager==4.1.2

# Security floors on transitive dependencies, mirroring the block in
# pyproject.toml. Not imported directly; pinned so pip cannot resolve onto a
# version with a known advisory. Each corresponds to a Dependabot alert open
# against poetry.lock on 2026-08-16.
h11>=0.16.0
Jinja2>=3.1.6
Werkzeug>=3.1.8
urllib3>=2.7.0
idna>=3.18
python-dotenv>=1.2.3
